/**
 * Contains interfaces for interacting with Teams, including ITeam (the 
 * actual Team object itself) and IMsgReceiver (the adapter that allows a user
 * to communicate with a remote Team object). 
 */
package common.api.team;